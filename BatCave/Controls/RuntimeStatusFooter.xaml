<UserControl
    x:Class="BatCave.Controls.RuntimeStatusFooter"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Spacing="6">
        <TextBlock Style="{StaticResource BatCaveMetricLabelStyle}" Text="DIAGNOSTICS" />
        <StackPanel Orientation="Horizontal" Spacing="8">
            <TextBlock FontWeight="SemiBold" Text="Runtime Alert:" />
            <TextBlock
                AutomationProperties.LiveSetting="Polite"
                AutomationProperties.Name="Runtime Status Announcement"
                Opacity="0.82"
                Text="{Binding RuntimeStatusSummary, Mode=OneWay}"
                TextWrapping="Wrap" />
        </StackPanel>
        <StackPanel Orientation="Horizontal" Spacing="8">
            <TextBlock FontWeight="SemiBold" Text="Runtime Detail:" />
            <TextBlock
                AutomationProperties.Name="Runtime Health Status"
                Opacity="0.82"
                Text="{Binding RuntimeHealthStatus, Mode=OneWay}"
                TextWrapping="Wrap" />
        </StackPanel>
        <TextBlock
            AutomationProperties.Name="Interaction Timing Probe"
            FontFamily="Cascadia Mono"
            Opacity="0.8"
            Text="{Binding InteractionTimingProbe, Mode=OneWay}"
            TextWrapping="WrapWholeWords" />
    </StackPanel>
</UserControl>
