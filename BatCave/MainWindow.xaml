<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="BatCave.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="BatCave">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <ScrollViewer>
            <StackPanel Margin="16" Spacing="12">
                <TextBlock FontSize="24" FontWeight="SemiBold" Text="BatCave Monitor" />
                <TextBlock Opacity="0.8" Text="Windows-native process telemetry with anti-lag table updates." />

                <TextBlock Text="Filter" />
                <TextBox
                    PlaceholderText="name or PID"
                    Text="{x:Bind ViewModel.FilterText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                <StackPanel Orientation="Horizontal" Spacing="12">
                    <ToggleSwitch
                        x:Name="AdminModeToggle"
                        Header="Admin Mode"
                        IsEnabled="{x:Bind ViewModel.IsLive, Mode=OneWay}"
                        IsOn="{x:Bind ViewModel.AdminModeEnabled, Mode=OneWay}"
                        Toggled="AdminModeToggle_Toggled" />
                    <CheckBox
                        VerticalAlignment="Center"
                        Content="Admin-enabled only"
                        IsChecked="{x:Bind ViewModel.AdminEnabledOnlyFilter, Mode=TwoWay}"
                        IsEnabled="{x:Bind ViewModel.AdminModeEnabled, Mode=OneWay}" />
                </StackPanel>

                <TextBlock
                    Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                    Text="{x:Bind ViewModel.AdminModeError, Mode=OneWay}" />

                <Border
                    Padding="20"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="12">
                    <StackPanel Spacing="8">
                        <TextBlock FontSize="26" FontWeight="SemiBold" Text="{x:Bind ViewModel.ShellHeadline, Mode=OneWay}" />
                        <TextBlock TextWrapping="Wrap" Text="{x:Bind ViewModel.ShellBody, Mode=OneWay}" />
                    </StackPanel>
                </Border>

                <TextBlock FontWeight="SemiBold" Text="Visible Rows" />
                <ItemsControl ItemsSource="{x:Bind ViewModel.VisibleRows, Mode=OneWay}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Border
                    Padding="12"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="12">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock FontWeight="SemiBold" Text="Runtime Health:" />
                        <TextBlock Text="{x:Bind ViewModel.RuntimeHealthStatus, Mode=OneWay}" TextWrapping="Wrap" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
